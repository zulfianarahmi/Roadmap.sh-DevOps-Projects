# GitHub Deployment Workflow

This project demonstrates a GitHub Action workflow to deploy a simple website to GitHub Pages.

## Features
- Deploys the `index.html` file to GitHub Pages.
- Triggers deployment only when `index.html` is modified.

## Usage
Push changes to the `index.html` file in the `main` branch to deploy the updated website.
